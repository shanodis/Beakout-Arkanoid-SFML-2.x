/*Menu menu;
	bool creditsPage = false,
		multiplayerPage = false;

	while (window.isOpen())
	{
		// Obsługa zdarzeń
		Event event;
		while (window.pollEvent(event))
		{
			if (event.type == Event::Closed)
			{
				window.close();
				return false;
			}

			// Obsługa klawiatury w menu

			if (event.type == Event::KeyPressed && event.key.code == Keyboard::Escape)
			{
				if (!EscapeButton(window, menu, creditsPage)) return false;
			}

			if (event.type == Event::KeyPressed && event.key.code == Keyboard::Enter)
			{
				int choice = EnterButton(menu, creditsPage);
				if (choice == 1) return true;
				else if (choice == 3) return false;
			}

			if ((event.type == Event::KeyPressed && event.key.code == Keyboard::Down || event.key.code == Keyboard::Up)
				&& (creditsPage == false))
				menu.moveArrow();
		}
		// Aktualizacja menu...
		menu.moveLandscape();

		// Rysowanie sceny
		window.clear(Color(219, 176, 239)); // Kolor wrzosowy

		menu.draw(window, creditsPage);

		window.display();
	}
	return true;*/